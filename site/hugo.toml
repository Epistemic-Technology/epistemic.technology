baseURL = 'https://epistemic.technology'
languageCode = 'en-us'
title = 'Epistemic Technology'
theme = 'ETBASIC'

[markup]
  [markup.goldmark]
    [markup.goldmark.parser]
      attribute = true
    [markup.goldmark.renderer]
      unsafe = true  # This allows raw HTML

[params]
  # Use getenv with a default value as fallback
  formSubmitEndpoint ="http://localhost:8080/" 